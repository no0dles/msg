import { ListenerContext } from "@msg/core";
import { MessageHandle } from "@msg/message";

export class ValidatorHandle extends MessageHandle<any> {
  run(message: any, context: ListenerContext<any>): void {

  }
}
